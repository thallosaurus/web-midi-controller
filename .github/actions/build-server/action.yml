name: "Build Server"
description: "Build Server"

inputs:
    target:
        required: true

runs:
    using: "composite"
    steps:
      - name: Build Server
        shell: bash
        working-directory: ./homebrewdj
        #run: deno task compile:${{ inputs.target }}:release
        run: deno bundle --minify main.ts > ../dist/bundled.js